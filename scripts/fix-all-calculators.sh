#!/bin/bash
# Fix all calculator import forms to match paver pattern
# This script will be used as a reference - edits done via replace_string_in_file

echo "Calculators to fix:"
echo "1. mulching - needs full paver pattern"
echo "2. pine_needles - needs full paver pattern"
echo "3. pruning - needs full paver pattern"  
echo "4. turf-mowing - needs full paver pattern"
echo "5. syn-turf - needs full paver pattern"
echo "6. fence - different structure, check if working"
echo "7. retaining-wall - uses $est->title already, check if has other issues"
